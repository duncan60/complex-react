import React, { PropTypes, Component } from 'react';

class Firebase extends Component {
    render() {
        return (
            <div>
                Firebase
            </div>
        );
    };
};

export default Firebase;