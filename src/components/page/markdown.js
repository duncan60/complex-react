import React, { PropTypes, Component } from 'react';

class Markdown extends Component {
    render() {
        return (
            <div>
                Markdown
            </div>
        );
    };
};

export default Markdown;