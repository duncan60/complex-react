import * as types from '../constants/ActionTypes';

let initialState = {};

const Index = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    };
};

export default Index;
